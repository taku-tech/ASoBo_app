<div class="row">
	<div class="col-xs-5">
		<%= form_with model: @admin, url: admins_admin_path(@admin.id), method: :patch, local: true do |f| %>
		<%= f.label :email, "メールアドレス" %>
		<%= f.email_field :email %>
		<%= f.submit "更新" %>
		<% end %>
	</div>
</div>