<div class="container">
	<div class="row">
		<div class="col-xs-5">
			<%= form_with model: @user, url: user_path(@user.id), method: :patch, local: true do |f| %>
			<%= f.label :name, "ユーザ名" %>
			<%= f.text_field :name %>
			<%= f.submit "更新" %>
			<% end %>
		</div>
	</div>
</div>